const people = ['Abe', 'Benny', 'Carson'];
const ages = [5, 10, 15];
console.log(people);

// the following line will set the export value for the variable
// i.e. sets the value of {variable} to be equal to the {people} array
module.exports = {people, ages};